import { Tube } from '../component';
/**
 * A component that converts raw binary MP4 data into ISOM boxes.
 * @extends {Component}
 */
export declare class Mp4Parser extends Tube {
    /**
     * Create a new RTSP parser component.
     */
    constructor();
}
