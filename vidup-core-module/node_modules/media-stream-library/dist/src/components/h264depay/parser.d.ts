import { H264Message, RtpMessage } from '../message';
export declare enum NAL_TYPES {
    UNSPECIFIED = 0,
    NON_IDR_PICTURE = 1,// P-frame
    IDR_PICTURE = 5,// I-frame
    SPS = 7,
    PPS = 8
}
export declare class H264DepayParser {
    private _buffer;
    constructor();
    parse(rtp: RtpMessage): H264Message | null;
}
